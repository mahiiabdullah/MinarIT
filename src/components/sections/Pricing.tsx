// Pricing — three-tier pricing cards with highlighted "popular" tier
"use client";

export default function Pricing() {
  return <section id="pricing">{/* Pricing section — coming next */}</section>;
}
