// CTA — full-width call-to-action section with gradient background and booking button
"use client";

export default function CTA() {
  return <section id="cta">{/* CTA section — coming next */}</section>;
}
